{% extends "::layout.html.twig" %}

{% block breadcrumb %}
	{{ parent() }}
	> {{ 'index.profileof'|trans({'%name%': user.getUsername()})}}
{% endblock %}

{% block body %}
<section id="identite">
	<header>
		<h1>{{ 'index.indentie' | trans() }}</h1>
	</header>
	<section>
		<table>
			<tr><td>{{ 'index.registeredon' | trans() }} :</td> <td>{{ user.getInscription() }}</td></tr>
			<tr><td>{{ 'index.nbvisite' | trans() }} :</td> <td>{{ user.getNbvisite() }}</td></tr>
			<tr><td>{{ 'index.point' | trans() }} :</td> <td>{{ user.getPoint() }}</td></tr>
			<tr><td>{{ 'index.born' | trans() }} :</td> <td>{{ user.getBorn() }}</td></tr>
			<tr><td>{{ 'index.location' | trans() }} :</td> <td>{{ user.getLieu() }}</td></tr>
			<tr><td>{{ 'index.guilde' | trans() }} :</td> <td>{{ user.getGuilde() }}</td></tr>
			<tr><td>{{ 'index.site' | trans() }} :</td> <td>{{ user.getSite() }}</td></tr>
		</table>
	</section>

	<footer>
    	<h1>Badges:</h1>
	</footer>
</section>


<section id="identite">
	<h1>{{ 'index.personnages' | trans() }}</h1>	
</section>
{% endblock %}