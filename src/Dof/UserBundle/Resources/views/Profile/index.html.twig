{% extends "::layout.html.twig" %}

{% block breadcrumb %}
	{{ parent() }}
	> {{ 'of'|trans({'%name%': user.username }, 'profile')}}
{% endblock %}

{% block body %}
<section id="identite">
	<section>
		<table>
			<tr><td>{{ 'inputs.nbvisits' | trans({}, 'profile') }} :</td> <td>{{ user.nbvisite }}</td></tr>
			<tr><td>{{ 'inputs.point' | trans({}, 'profile') }} :</td> <td>{{ user.point }}</td></tr>
			<tr><td>{{ 'inputs.date.born' | trans({}, 'profile') }} :</td> <td>{{ user.born }}</td></tr>
			<tr><td>{{ 'inputs.location' | trans({}, 'profile') }} :</td> <td>{{ user.lieu }}</td></tr>
			<tr><td>{{ 'inputs.guild' | trans({}, 'profile') }} :</td> <td>{{ user.guilde }}</td></tr>
			<tr><td>{{ 'inputs.website' | trans({}, 'profile') }} :</td> <td>{{ user.site }}</td></tr>
		</table>
	</section>

	<footer>
    	<h1>{{ 'inputs.website' | trans({}, 'profile') }}:</h1>
	</footer>
</section>

<section id="identite">
	<h1>{{ 'inputs.badges' | trans({}, 'profile') }}</h1>	
</section>

<section id="identite">
	<h1>{{ 'inputs.persos' | trans({'%name%': user.username }, 'profile') }}</h1>	
</section>
{% endblock %}