{% extends '::layout.html.twig' %}

{% block body %}
{% autoescape  %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{{ item.getName(locales())}}</h3>
        </div>
        <div class="panel-body">
            Panel content
        </div>
    </div>
{% endautoescape %}
{% endblock %}
