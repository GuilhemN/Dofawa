{% embed "DofItemsBundle:Templates:layout.html.twig" %}
    {% block name %}
        {% if item.name is not null %}
            {{ item.name }} ({{ itemTemplate.getName(locales()) }})
        {% else %}
            {{ itemTemplate.getName(locales()) }}
        {% endif %}
    {% endblock %}
{% endembed %}
