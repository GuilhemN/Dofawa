{% extends '::layout.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ set.getName(locales()) }}
{% endblock %}

{% block body %}
    {{ set }} ({{ items_count }} items) - lvl {{ level }}

    {% for item in items %}
    {% endfor %}

    {% for combination in combinations %}
    {% endfor %}
{% endblock %}
