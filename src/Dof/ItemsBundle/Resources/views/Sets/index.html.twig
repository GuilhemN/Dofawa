{% extends '::layout.html.twig' %}

{% block body %}
    <div class="panel panel-default">
        <div class="panel-heading">Les panoplies</div>

        <table class="table">
            <thead>
                <tr>
                    <th>{{ 'name' | trans({}, 'generalTrans')}}</th>
                    <th>{{ 'list.level' | trans({}, 'item')}}</th>
                    <th>Items</th>
                    <th>Éléments</th>
                </tr>
            </thead>
            <tbody>
                {% for set in sets %}
                    <tr>
                        <td>
                            <a href="{{ path('dof_set_show', {'slug': set.slug}) }}">
                                {{ set.getName(locales()) }}
                            </a>
                        </td>
                        <td>{{ 'level' | trans({'%level%': set.level}, 'item') }}</td>
                        <td>{{ set.itemCount }} items</td>
                        <td>
                            {% for element in set.elements %}
                                <img src="{{ asset('bundles/dofitems/img/' ~ element ~ '.png') }}" alt="{{ element }}">
                            {% endfor %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
