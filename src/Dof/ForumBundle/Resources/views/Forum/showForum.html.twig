{% extends 'DofArticlesBundle::layout.html.twig' %}

{% block breadcrumb %}
  <li><a href="{{ path('dof_forum_homepage') }}">Forum</a></li> <li>{{ forum.getName(locales()) }}</li>
{% endblock %}

{% block body %}
{% autoescape %}
	<div class="panel panel-default">
		<div class="panel-heading">
			<h3 class="panel-title">{{ forum.getName(locales()) }}</h3>
		</div>
		<table class="table">
			<tr><th>Topic</th><th>par</th><th>le</th><th>Nb. de réponse</th></tr>
			{% for topic in forum.topics %}
				<tr><td><a href="{{ path('dof_forum_show_topic', { 'slug' : topic.slug }) }}">{{ topic.getName(locales()) }}</a></td><td><a href="{{ path('dof_profile_userpage', { 'slug' : topic.owner }) }}">{{ topic.owner }}</a></td><td>{{ topic.createdAt | date("h:i:s d/m/Y") }}</td><td></td></tr>
			{% endfor %}
		</table>
	</div>
{% endautoescape %}
{% endblock %}