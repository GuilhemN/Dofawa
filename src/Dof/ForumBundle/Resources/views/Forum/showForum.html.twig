{% extends 'DofArticlesBundle::layout.html.twig' %}

{% block breadcrumb %}
  <li><a href="{{ path('dof_forum_homepage') }}">Forum</a></li> <li>{{ forum.getName(locales()) }}</li>
{% endblock %}

{% block body %}
{% autoescape %}
	<div class="panel panel-default">
		<div class="panel-heading">
			<h3 class="panel-title">{{ forum.getName(locales()) }}</h3>
		</div>
		<table class="table">
			<tr><th>Topic</th><th>par</th><th>Nb. de réponse</th></tr>
			{% for topic in forum.topics %}
				<tr><td>{{ topic.getName(locales()) }}</td> <td><a href="{{ path('dof_profile_userpage', { 'slug' : topic.owner }) }}">{{ topic.owner }}</a></td></tr>
			{% endfor %}
		</table>
	</div>
{% endautoescape %}
{% endblock %}